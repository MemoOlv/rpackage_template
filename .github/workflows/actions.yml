name: Tests
on: push
jobs:
  actions:
    runs-on: ubuntu-latest
    steps:
    - name: Copia repositorio
      uses: actions/checkout@main
    - name: Construye imagen
      run: docker build --tag memoolv/rpackage_template .
    - name: Verifica el formato
      run: docker run memoolv/rpackage_template make check
    - name: Corre pruebas y evalúa cobertura
      run: docker run memoolv/rpackage_template make coverage
    - name: Corre mutation testing
      run: docker run memoolv/rpackage_template make mutants
    - name: Create/Update tag
      uses: rickstaa/action-create-tag@v1.5.4
      with:
        tag: "latest"
        message: "Latest release"
        force_push_tag: true
